# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/
*.egg
.venv/
venv/
env/

# Sensitive files
users.json
config.json
mcp_keys.json
# Playwright MCP scratch output — claude-desktop-config.json in here carries a
# live X-MCP-Key header, so the whole directory stays out of the repository.
.playwright-mcp/
*.env
.env
.env.*
# ...but the templates must ship: README's Quick Start tells you to copy them,
# and `.env.*` above was silently excluding .env.example from the repository.
!.env.example
!users.json.example
!config.json.example

# Logs
logs/
*.log
*.jsonl

# macOS
.DS_Store
.AppleDouble
.LSOverride

# IDE
.vscode/
.idea/
*.swp
*.swo

# Junk artifacts
=*
{*

# Jupyter
.ipynb_checkpoints/

# Training / model files
training/
*.pkl
*.model
docs/**
# Claude Code worktrees — scratch working copies, never part of the repo
.claude/worktrees/

# Subagent-driven-development scratch workspaces (ledger, briefs, review packages)
.superpowers/
